[project]
name = "obdschat"
version = "0.1.0"
description = "Source-grounded chat for the German oBDS"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE", "THIRD_PARTY_NOTICES.md"]
requires-python = ">=3.13.9"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
    "uvicorn>=0.52.1",
]

[dependency-groups]
backend = [
    "beautifulsoup4>=4.15.0",
    "lxml>=6.1.1",
    "openai>=2.53.0",
    "psycopg[binary]>=3.3.4",
    "pydantic-settings>=2.15.0",
    "xmlschema>=4.3.2",
]
frontend = [
    "gradio>=6.22.0",
]
dev = [
    { include-group = "backend" },
    { include-group = "frontend" },
    "matplotlib>=3.11.1",
    "pytest",
    "pyyaml>=6.0.3",
    "ruff",
    "ty",
]
docs = [
    "mkdocs-material>=9,<10",
    "mkdocs-render-swagger-plugin>=0.1.2,<0.2",
]

[project.scripts]
obdschat-sync-sources = "scripts.sync_sources:main"

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
markers = [
    "db_smoke: requires TEST_DATABASE_URL pointing to PostgreSQL",
]

[tool.ruff]
exclude = ["src/backend/config_2.py"]
target-version = "py313"

[tool.ty.src]
exclude = ["src/backend/config_2.py"]

[tool.hatch.build]
exclude = ["/src/backend/config_2.py"]

[tool.hatch.build.targets.wheel]
packages = ["src/backend", "src/frontend", "src/obdschat_api", "scripts"]

[tool.hatch.build.targets.wheel.force-include]
"db/init.sql" = "scripts/data/init.sql"
